# サービス名
spring:
    application:
        name: discovery-service

# ConfigServiceのURI
    cloud:
        config:
            uri: http://localhost:8888
            profile: dev
            label: master
            name: discovery-service
